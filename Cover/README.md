# Cover

Place the cover artwork here as single-page PDFs:

- `Voorkant.pdf` — front cover
- `Achterkant.pdf` — back cover
- `Omslag.pdf` — full wrap-around cover for the printer (not included by LaTeX)

They are only included when `\def\setcover{true}` is set in `Thesis.tex`.
